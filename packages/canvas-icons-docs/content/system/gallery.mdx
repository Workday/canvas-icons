import * as systemIcons from '@workday/canvas-system-icons-web';

import {IconsGallery} from '../../components/IconsGallery';

# Gallery

### Active Icons

<br />

<IconsGallery
  icons={Object.keys(systemIcons)
    .filter(k => !('fallback' in systemIcons[k]) && 'svg' in systemIcons[k])
    .map(k => systemIcons[k])}
  type="system"
/>
